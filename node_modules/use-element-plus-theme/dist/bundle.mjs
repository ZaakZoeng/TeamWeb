import { onBeforeMount as P } from "vue";
const e = "--el-color-primary", E = `${e}-light`, M = `${e}-dark`, _ = "#ffffff", $ = "#000000", r = document.documentElement, o = (n, s, t) => {
  t = Math.max(Math.min(Number(t), 1), 0);
  const u = parseInt(n.substring(1, 3), 16), a = parseInt(n.substring(3, 5), 16), m = parseInt(n.substring(5, 7), 16), i = parseInt(s.substring(1, 3), 16), p = parseInt(s.substring(3, 5), 16), b = parseInt(s.substring(5, 7), 16), f = Math.round(u * (1 - t) + i * t), g = Math.round(a * (1 - t) + p * t), I = Math.round(m * (1 - t) + b * t), d = ("0" + (f || 0).toString(16)).slice(-2), l = ("0" + (g || 0).toString(16)).slice(-2), y = ("0" + (I || 0).toString(16)).slice(-2);
  return "#" + d + l + y;
}, c = (n) => {
  if (!n)
    return;
  r.style.setProperty(e, n);
  for (let t = 1; t < 10; t += 1)
    r.style.setProperty(`${E}-${t}`, o(n, _, t * 0.1));
  const s = o(n, $, 0.2);
  r.style.setProperty(`${M}-2`, s);
};
function T(n) {
  return P(() => c(n)), {
    changeTheme: c
  };
}
export {
  T as useElementPlusTheme
};
